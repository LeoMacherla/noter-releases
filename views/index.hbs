<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Noter Releases</title>

    <style>
      	:root {

          --theme-hue: 264;

        --primary-900: var(--theme-hue) 46% 47%;
        --primary-800: var(--theme-hue) 49% 52%;
        --primary-700: var(--theme-hue) 60% 57%;
        --primary-600: var(--theme-hue) 76% 63%;
        --primary-500: var(--theme-hue) 100% 69%;
        --primary-400: var(--theme-hue) 100% 72%;
        --primary-300: var(--theme-hue) 100% 75%;
        --primary-200: var(--theme-hue) 100% 77%;
        --primary-100: var(--theme-hue) 100% 80%;

        --bg-900: var(--theme-hue) 12% 10%;
        --bg-800: var(--theme-hue) 12% 11%;
        --bg-700: var(--theme-hue) 11% 12%;
        --bg-600: var(--theme-hue) 10% 14%;
        --bg-500: var(--theme-hue) 11% 15%;
        --bg-400: var(--theme-hue) 10% 18%;
        --bg-300: var(--theme-hue) 7% 25%;
        --bg-200: var(--theme-hue) 5% 32%;
        --bg-100: var(--theme-hue) 4% 38%;

      	--text: var(--theme-hue) 0% 95%;
      	--text-muted: var(--text) / 0.6;
      }

      body {
        box-sizing: border-box;
        padding: 20px;
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont,
        "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell,
        "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif;
        background: hsl(var(--bg-600));
        color: hsl(var(--text));
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
        text-rendering: optimizeLegibility;
        height: 100vh;
      }

      #wrap {
        display: flex;
        align-items: center;
        height: 100%;
      }

      main {
        width: 500px;
        margin: 0 auto;
      }

      header {
        display: flex;
        align-items: center;
        font-size: 24px;
        margin-bottom: 40px;
      }

      #release {
        flex-grow: 1;
      }

      #repo {
        font-weight: 800;
      }

      #date {
        color: hsl(var(--text-muted));
        font-size: 18px;
      }

      .item {
        display: flex;
        align-items: center;
      }

      .item:not(:last-of-type) {
        margin-bottom: 14px;
      }

      .item a {
        color: hsl(var(--primary-500));
        text-decoration: none;
      }

      .item a:hover {
        text-decoration: underline;
      }

      .fileType {
       font-weight: 600;
       flex-grow: 1;
      }

      .url {
        font-weight: 400;
      }

      .size {
        color: hsl(var(--text) / 0.3);
        white-space: nowrap;
      }

      footer {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
      }

      footer > * {
        margin-top: 40px;
      }

      footer a {
        color: hsl(var(--text-muted));
        text-decoration: none;
      }

      footer a:hover {
        text-decoration: underline;
      }

      #version {
        margin-right: 8px;
        padding: 2px 6px;
        border-radius: 4px;
        background: linear-gradient(hsl(var(--primary-700)), hsl(var(--primary-400)))
      }

      #release-notes {
        font-weight: 600;
        color: hsl(var(--text));
      }

      #all-releases {
        text-align: center;
        flex-grow: 1;
      }
    </style>
  </head>

  <body>
    <div id="wrap">
      <main>
          <header>
            <div id="release">Noter Releases</div>
            <div id="date">{{date}}</div>
          </header>

          <div id="list">
            {{#each files}}
              <div class="item">
                <div class="fileType">{{@key}}: <span class="url"><a href={{this.url}}>/latest/{{@key}}</a></span></div>
                <div class="size">{{this.size}} MB</div>
              </div>
            {{/each}}
          </div>

          <footer>
            <div id="version">{{version}}</div>
            <a id="release-notes" href={{releaseNotes}}>Release Notes</a>
            <a id="all-releases" href={{allReleases}}>All Releases</a>
            <a href={{github}}>GitHub</a>
          </footer>
      </main>
    </div>
  </body>
</html>
